package com.app.syspoint.utils

/*class TokenCreator {
    companion object {
        private const val API_LEVEL = "2"
        private const val appName = BuildConfig.APPLICATION_ID
        private const val password = "%AppSyspoint2023%*"

        private val buf = CharArray(SECURE_TOKEN_LENGTH)

        fun createToken(): String {
            for (idx in 0..buf.size) buf[idx] = symbols[random.nextInt(symbols.size)]
            return String(buf)
        }
    }
}*/